import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Surfaces
  static const Color background = Color(0xFF0F0F12);
  static const Color surface = Color(0xFF1A1A1F);
  static const Color surfaceAlt = Color(0xFF26262B);
  static const Color inputFill = Color(0xFF2A2A30);
  static const Color border = Color(0xFF2A2A30);
  static const Color divider = Color(0xFF26262B);

  // Brand
  static const Color primary = Color(0xFF3528E0);
  static const Color primaryDim = Color(0xFF241D7A);
  static const Color primaryDark = Color(0xFF221AA8);
  static const Color accentBlue = Color(0xFF2B7DFF);

  // Income / Expense gradients
  static const Color incomeStart = Color(0xFF14532D);
  static const Color incomeEnd = Color(0xFF043315);
  static const Color expenseStart = Color(0xFF7F1D1D);
  static const Color expenseEnd = Color(0xFF3F0A0A);

  // Inline credit/debit text
  static const Color credit = Color(0xFF22C55E);
  static const Color debit = Color(0xFFEF4444);

  // Text
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF9CA3AF);
  static const Color textMuted = Color(0xFF6B7280);
  static const Color textPlaceholder = Color(0xFF6B7280);

  // Splash logo
  static const Color logoLight = Color(0xFF0F7FFF);
  static const Color logoDark = Color(0xFF2A20D6);

  // Shimmer
  static const Color shimmerBase = Color(0xFF1A1A1F);
  static const Color shimmerHighlight = Color(0xFF26262B);
}
